import { Subtitle } from '../types/types';

export class SrtParser {
    static parse(content: string): Subtitle[] {
        // 实现SRT解析逻辑
        return [];
    }

    static stringify(subtitles: Subtitle[]): string {
        // 实现SRT生成逻辑
        return '';
    }
}
